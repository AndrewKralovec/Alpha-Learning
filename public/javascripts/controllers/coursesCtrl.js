/* Andrew Kralovec 
 * Courses controller 
 * Logic written in Javascript, responsible for formatting what the user sees on the Courses page
 * This, for the moment, includes the count of courses,and at which index to split the courses up on the page 
 */
var app = angular.module('coursesModule', []);
app.controller('coursesCtrl', function ($scope, $http) {
});